mod animation; // u have models animation
// and u will have mod.rs in animation
use animation::animation::run; // using run
fn main() -> Result<(), String> {
    run()
}
/*
src/
├── main.rs
└── animation/
    └── animation.rs
 u will have this struct to run animation
 */

 